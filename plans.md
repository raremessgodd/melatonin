# 🔬 Аудит проекта melatonin*

Проект сделан аккуратно — видна продуманная архитектура. Но есть несколько уровней улучшений, от архитектурных до мелких, каждый из которых реально влияет на производительность, поддерживаемость и UX.

---

## 🏗️ 1. АРХИТЕКТУРНЫЕ ПРОБЛЕМЫ

### 1.1 Страница `about.astro` — жёстко зашитый HTML вместо данных
Слайды `PhotoStack` захардкожены прямо в `.astro` файле — 4 секции с текстом и картинками написаны как сырой HTML. При этом есть система `farewellsSections.ts` + `Section.astro` + `Block.astro` с типизированными данными. Это нарушение принципа единообразия: `about` выпадает из общей CMS-подобной системы.

**Что сделать:** создать `aboutSections.ts` (файл уже есть, но пустой!), добавить тип блока `photoStack` в `sections.ts` и вынести слайды туда. Страница `about.astro` должна рендериться через тот же `Section` + `Block`.

### 1.2 Навигационные ссылки продублированы в двух местах
В `BaseLayout.astro` ссылки (FAREWELLS, ABOUT, соцсети) написаны дважды — в `header` и в `.mobile-menu`. Если добавится новый раздел — нужно менять в двух местах.

**Что сделать:** вынести nav в отдельный компонент `Navigation.astro` с пропсом `variant: 'desktop' | 'mobile'`.

### 1.3 `aboutSections.ts` пустой, но импортируется
Файл существует, экспортирует пустой массив. `about.astro` его не использует вообще. Это мёртвый код, вводящий в заблуждение.

### 1.4 `sections.ts` (главная страница) содержит только одну секцию
Главная по сути пустая — только intro с текстом. Это либо незавершённая фича, либо намеренный минимализм, но тогда секционная система избыточна для одного блока.

---

## ⚡ 2. ПРОИЗВОДИТЕЛЬНОСТЬ

### 2.1 Canvas-фон тайлится CPU — нет GPU-ускорения
`BackgroundCanvasManager` рисует сетку тайлов через `drawImage()` в цикле на каждый `ResizeObserver` вызов. Нет `will-change`, нет `OffscreenCanvas`, нет `ImageBitmap`.

**Что сделать:** использовать `createImageBitmap()` для кэширования одного tile, а лучше — заменить canvas на CSS `background-image` с `background-size` и `background-repeat: repeat`, анимируя только `background-position` через CSS transitions. Это полностью снимет нагрузку с CPU.

### 2.2 Два canvas для crossfade — это 2× память и 2× drawCalls
`PanelTransitionManager` создаёт два canvas (A и B) для плавного перехода фонов. Это дорого по памяти при больших изображениях.

**Что сделать:** crossfade через CSS `opacity` transition на двух `<img>` тегах с `will-change: opacity` — браузер сам справляется через compositor thread без JS.

### 2.3 Шрифты загружаются как 5 отдельных CSS-файлов
```css
@import '@fontsource/space-grotesk/300.css';
@import '@fontsource/space-grotesk/400.css';
@import '@fontsource/space-grotesk/500.css';
@import '@fontsource/syne/700.css';
@import '@fontsource/syne/800.css';
```
Каждый `@fontsource` CSS — это ещё один HTTP-запрос на `.woff2`. 5 запросов на шрифты блокируют рендер.

**Что сделать:** добавить `<link rel="preload" as="font">` для критичных начертаний в `<head>`. Или перейти на `@fontsource/variable` пакеты (один файл покрывает весь диапазон весов).

### 2.4 Нет `srcset` и `<picture>` для изображений
Логика адаптивных изображений полностью на клиенте (`getResponsiveSrc` в `MediaBlock.astro` + `LazyMediaManager`). SSG мог бы генерировать `srcset` статически — браузер сам выберет нужный размер ещё до JS.

**Что сделать:** использовать `@astrojs/image` (встроен в Astro 5) или `<Picture>` компонент из `astro:assets` для автоматической генерации WebP + нескольких размеров. Это убирает `LazyMediaManager.loadImage` полностью.

### 2.5 `amplitudejs` (5.3.2) — тяжёлая зависимость для одного трека
AmplitudeJS весит ~30 KB gzipped. Для одного аудио-трека это избыточно. Весь `AudioPlayerManager.js` компенсирует слабости Amplitude (ручной `timeupdate`, MutationObserver для класса).

**Что сделать:** заменить на нативный `<audio>` + ~50 строк кастомного кода. Или использовать легковесную альтернативу `howler.js` (если нужны кроссфейды).

---

## 🧱 3. TypeScript И ТИПОБЕЗОПАСНОСТЬ

### 3.1 Весь JS (`App.js`, менеджеры) — без типов
`tsconfig.json` есть, типы описаны в `src/types/sections.ts`, но сами менеджеры — чистый JS без JSDoc или `.ts` расширений. Ошибки типов не ловятся.

**Что сделать:** переименовать менеджеры в `.ts`, добавить интерфейс `IManager` с `destroy()`, типизировать конструкторы.

### 3.2 `Block.astro` использует `block.type === 'video'` как алиас для `MediaItem`
```js
{(block.type === 'media' || block.type === 'video') && <MediaBlock ... />}
```
Это костыль — `VideoBlock` и `MediaItem` — разные типы, но рендерятся одинаково через spread `{ ...block, mediaType: 'video' }`. Должен быть один тип `MediaBlock` с `mediaType`.

### 3.3 `about.css` загружается через `?url` + динамический `<link>`
```js
import aboutCss from '../styles/components/about.css?url';
// ...
<link slot="styles" rel="stylesheet" href={aboutCss} />
```
Это нестандартный паттерн — CSS не обрабатывается Vite pipeline (нет autoprefixer, нет css-modules). Лучше импортировать напрямую в компоненте.

---

## 🎨 4. CSS АРХИТЕКТУРА

### 4.1 `@layer` объявлен после `@import` — не работает так, как задумано
```css
@import 'base/variables.css';
/* ... */
@layer tokens, base, layout, components, utilities;
```
Объявление `@layer` **после** `@import` не назначает импортированные стили в слои. Стили из импортов попадают в "неименованный" слой с наивысшим приоритетом. Система слоёв существует в декларации, но де-факто не работает.

**Что сделать:** обернуть каждый `@import` в `@layer`:
```css
@layer base { @import 'base/reset.css'; }
@layer tokens { @import 'base/variables.css'; }
```

### 4.2 Нет `prefers-reduced-motion`
В `animations.css` есть анимации, но нет `@media (prefers-reduced-motion: reduce)` — пользователи с вестибулярными нарушениями получат все эффекты без возможности их отключить.

### 4.3 `user-scalable=no` в viewport мета-теге — нарушение a11y
```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
```
Это запрещает пользователям масштабировать страницу. Нарушает WCAG 1.4.4 (AA). Apple также игнорирует это в iOS 10+, так что и смысла нет.

---

## ♿ 5. ДОСТУПНОСТЬ (A11Y)

### 5.1 `<h1>` используется три раза на странице `farewells`
В `farewellsSections.ts` три блока имеют `titleTag: 'h1'` — "Farewells", "Promo", "Live Performance". На странице должен быть один `<h1>`.

### 5.2 Лайтбокс не трапит фокус
`LightboxManager` открывает диалог, но не перемещает фокус внутрь и не трапит Tab. Пользователи клавиатуры могут взаимодействовать с контентом за лайтбоксом.

**Что сделать:** добавить `focus trap` при открытии (`dialog` элемент или `aria-modal` + ручной trap).

### 5.3 Видео-плеер: нет `<track>` для субтитров
Видео без субтитров нарушает WCAG 1.2.2 для предзаписанного контента.

---

## 🔧 6. DEVEX И ИНФРАСТРУКТУРА

### 6.1 Нет линтера и форматтера
В `package.json` нет ESLint, Prettier, Stylelint. Стиль кода разный: в `PhotoStackManager.js` отступы 2 пробела, в `ScrollManager.js` — 4 пробела, в `TerminalAnimationManager.js` — 4 пробела.

**Что сделать:** добавить `eslint` + `eslint-plugin-astro` + `prettier` + `stylelint`.

### 6.2 Нет тестов
Ни unit, ни e2e. Менеджеры с нетривиальной логикой (`PanelTransitionManager`, `PhotoStackManager`, `LightboxManager`) не покрыты.

**Что сделать:** хотя бы `vitest` для utils-функций (`getMaxQualityPath`, `getResponsiveSrc`), `playwright` для e2e.

### 6.3 Нет `check` скрипта в `package.json`
Нет `"check": "astro check"` — TypeScript-ошибки в `.astro` файлах не проверяются при CI.

### 6.4 Нет CI/CD конфигурации
Нет `.github/workflows` или любого другого CI. Деплой ручной.

---

## 🌐 7. SEO И МЕТАДАННЫЕ

### 7.1 Нет structured data (JSON-LD)
Для музыкальной группы базовый `MusicGroup` schema от schema.org значительно улучшает видимость в поиске. Google показывает расширенные результаты (карточка группы, соцсети, альбомы).

### 7.2 `robots.txt` отсутствует
В `public/` нет `robots.txt`. Без него поисковики индексируют всё подряд, включая `/assets/video/hls/` (тысячи `.ts` файлов!).

**Что сделать:** добавить `public/robots.txt`:
```
User-agent: *
Disallow: /assets/video/
Disallow: /assets/music/
```

### 7.3 Нет `manifest.json` для PWA
Сайт используется как мобильное приложение (`apple-mobile-web-app-title`), но нет `manifest.json` и иконок нужных размеров. PWA install prompt не появится.

---

## 📦 ИТОГ ПО ПРИОРИТЕТАМ

| Приоритет | Задача | Усилие |
|-----------|--------|--------|
| 🔴 Критично | `robots.txt` (индексация HLS) | 5 мин |
| 🔴 Критично | `user-scalable=no` убрать | 1 мин |
| 🔴 Критично | `<h1>` один на страницу | 10 мин |
| 🟠 Высокий | Замена canvas-фона на CSS background | 2–4 ч |
| 🟠 Высокий | `astro:assets` для изображений (WebP + srcset) | 2–3 ч |
| 🟠 Высокий | `prefers-reduced-motion` в CSS | 30 мин |
| 🟠 Высокий | Eslint + Prettier | 1 ч |
| 🟡 Средний | TypeScript для менеджеров | 3–5 ч |
| 🟡 Средний | `about.astro` → данные в `aboutSections.ts` | 2 ч |
| 🟡 Средний | Шрифты: variable fonts / preload | 1 ч |
| 🟡 Средний | Лайтбокс: focus trap | 1 ч |
| 🟢 Низкий | Замена AmplitudeJS на нативный audio | 2–3 ч |
| 🟢 Низкий | JSON-LD structured data | 1 ч |
| 🟢 Низкий | PWA manifest | 1 ч |
| 🟢 Низкий | CI/CD | 1–2 ч |
